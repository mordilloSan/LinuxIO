The official Codex manual confirms the distinction: reasoning effort and Fast mode are separate controls. medium is the balanced worker default; Fast mode is a service tier (service_tier = "fast" / /fast), not another name for low reasoning. The spawn control available in this chat did not expose a service-tier argument, so my earlier worker setup did not satisfy your instruction.