name: example-module
version: 1.0.0
title: Example Module
description: A demonstration UI module using existing LinuxIO handlers
author: LinuxIO Team
homepage: https://github.com/mordilloSan/LinuxIO
license: MIT

# UI Configuration
ui:
  route: /example-module           # Frontend route (must be unique)
  icon: "mdi:puzzle-outline"       # Material Design Icon
  sidebar:
    enabled: true                  # Show in sidebar
    position: 115                  # Sidebar position (lower = higher in list)

# This is a UI-only module that uses:
# 1. Existing LinuxIO handlers
# 2. Exec streams
