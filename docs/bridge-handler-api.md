# Bridge Handler API

The bridge handler API is documented in the canonical [API Contract](./api-contract.md).

Start there for:

- `apischema` route contracts
- typed handler, runner, and duplex signatures
- JSON stream-open envelopes
- job and stream route behavior
- adding a new endpoint

For handler layout, context propagation, logging, and file naming conventions, see [Handler Patterns](./BRIDGE_HANDLER_PATTERNS.md).
