import { createLink } from "@tanstack/react-router";

import AppLinkButton from "@/components/ui/AppLinkButton";

const AppRouterLinkButton = createLink(AppLinkButton);

export default AppRouterLinkButton;
