import React from "react";

const EmptyState: React.FC = () => {
  return (
    <div
      style={{
        padding: "32px",
        textAlign: "center",
        fontSize: "16px",
      }}
    >
      This folder is empty.
    </div>
  );
};

export default EmptyState;
