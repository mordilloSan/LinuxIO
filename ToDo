separate the bridge and server code
move all code for api and executions to the bridge.

implement wireguard
docker compose

bridge socket and bridge binary should not have the sessionid written 

use quantom filebrowser as our main navigator - DONE
when we login we must also add the user to filebrowser
when the bridge does cleanup we also must kill the PTY session
websocket should be permanent.
If no websocket present check for session. if session restore websocket connection if not logout?