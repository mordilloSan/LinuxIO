1 - separate the bridge and server code

2 - Main Server should be responsible for:

Session
Auth
Webserver
Global API

3 - The Global API must be able to do:

Public or Private access (based on auth)
Responsible for displaying data (doesnt perform actions) except for auth and theme

4 - implement wireguard

5 - docker compose

6 - bridge socket and bridge binary should not have the sessionid written

use quantom filebrowser as our main navigator - DONE
when we login we must also add the user to filebrowser - DONE
filebrowser header should contain sessionid - DONE
destroy the filebrowser config file after we create the container - DONE

update user config files locations for theme (after login) - We should only have 1 config file. that file should only exist after login... For main programa startup no config file should be needed...

when the bridge does cleanup we also must kill the PTY session - check when is needed

websocket should be persistent

If no websocket present check for session. if session restore websocket connection if not logout?
